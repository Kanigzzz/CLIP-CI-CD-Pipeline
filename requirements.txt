--extra-index-url https://download.pytorch.org/whl/cpu

# torch/torchvision: na Linuksie (Docker, CI) bierzemy jawnie buildy +cpu
# z indeksu PyTorcha - bez tego pip/uv wziąłby wersję z PyPI, która ciągnie
# ~2.5 GB zależności nvidia-cuda-*. Na macOS istnieją tylko zwykłe wheele.
torch==2.14.0+cpu; sys_platform == "linux"
torch==2.14.0; sys_platform != "linux"
torchvision==0.29.0+cpu; sys_platform == "linux"
torchvision==0.29.0; sys_platform != "linux"

transformers>=4.38.0
numpy>=1.24.0
pillow>=10.0.0
tqdm>=4.66.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
faiss-cpu>=1.8.0
pydantic>=2.0
pytest
pyyaml
huggingface_hub
onnx>=1.16.0
onnxruntime>=1.19.0
